services:
  backend:
    build:
      context: .
      target: production
    ports:
      - "5000:5000"
    environment:
      NODE_ENV: production
    restart: unless-stopped

  mongo:
    image: mongo:7
    ports:
      - "27019:27017"
    restart: unless-stopped
